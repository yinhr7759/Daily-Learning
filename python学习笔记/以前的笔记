1.字符串  
title() 首字母大写  ，不会改变存储在变量中的值  
upper()全大写  ，不会改变存储在变量中的值  
lower()全小写   ，不会改变存储在变量中的值  
rstrip（）删除末尾空白  
lstrip（）删除开头空白  
strip（）删除两端空白  
str（）将非字符串值转换为字符串  
2.列表  
append（）在末尾添加新元素  
insert（n，str）插入新元素  
del  data[n]删除元素  
pop（n）删除元素，并使用  ex：new=old.pop（n）  
remove（str）删除值为str的元素  
sort（）永久排序，从小到大；sort（reverse=True）永久排序，从大到小  
sorted（）临时排序，从小到大；sorted（reverse=True）永久排序，从大到小  
reverse（）反转列表  
len（）获取列表元素个数  
-1表示索引列表最后一个元素，-2倒数第二个，以此类推  
3.列表的操作  
遍历 ex：datas是一个列表 for data in datas：能将datas中每一个元素依次存储到data中  
range（）生成数值，ex：ranger（1，5）生成数组1 2 3 4 ；list（range（））创建数值列表  
min max sum可以求数字列表的最小最大总和  
列表解析，列表[表达式 循环]，ex：datas是一个列表，datas[data**2 for value data in range(1,11)]   
使用列表的一部分  datas[n:]，datas[:n]，datas[n:m]，复制整个列表datas[:]  
4.元组----不可变的列表，使用圆括号标识（）而不是[]  
虽然不能修改元素值，但可以改变整个元组  
元组的循环与列表的循环相同  
5.if语句  
and且      or或  
if elif else  
6.字典  
ex：data={'q1' : '1','q2' : 2},使用data['q1'],添加直接添加，删除del，字典中存储的是键值对，q1是键，1是值  
遍历字典中的所有键值对   ex：字典data中存在q，w，e，r四个键值对，for a，s，d，f in data（）即可遍历data中的所有键值对  
遍历字典中的所有键   ex：for name in data.keys（）
遍历字典中的所有值   ex：for name in data.values（）
set（data.values（））可以提取字典中所有独一无二的值  
字典和列表可以互相嵌套，字典中也可以嵌套字典  
7.输入  
input，可以接受一个输入提示的字符串参数 ex：input（str）
int（input（str））可以获得数值输入   
8.while循环  
break直接退出循环，continue不再执行当前循环后面的代码跳到循环开始  
9.函数  
def fun（）：  
           code  
           ......  
  











